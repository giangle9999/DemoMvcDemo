@model IEnumerable<Demo.Models.Demo123>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Demo123Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Demo123Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Demo123ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Demo123ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Demo123ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
