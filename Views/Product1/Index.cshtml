@model IEnumerable<Demo.Models.Product1>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Product1Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.category)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Product1Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.category.CategoryID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Product1ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Product1ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Product1ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
